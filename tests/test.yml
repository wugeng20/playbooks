- hosts: "{{ hosts }}"

  tasks:
    - name: touch test file
      command: touch /tmp/ansible_yml_test.txt
      tags: touch
    - name: echo ansible constainer
      command: echo "这是ansible一个测试" > /tmp/ansible_yml_test.txt
      tags: echo